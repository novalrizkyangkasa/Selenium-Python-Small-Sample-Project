[pytest]
addopts = -q
markers =
    smoke: quick end-to-end happy path
    regression: extended UI coverage
    auth: authentication flows
    cart: cart and checkout flows
    ui: modal/category/pagination flows
